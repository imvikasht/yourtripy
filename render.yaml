services:
  - type: web
    name: yourtripy-ai-trip-co-pilot
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 20.x
